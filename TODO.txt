TODO — MI Dépenses
==================
Légende : [ ] à faire  [~] en cours  [x] fait


── UX MOBILE ──────────────────────────────────────────────────────────────────

[x] Swipe entre les mois (navigation par geste)
    → initSwipe() dans app.js : touchstart/touchend sur document
    → |dx| > 60px ET |dx| > |dy| (horizontal) ET pas de modal ouvert
    → navigateMonth(±1) met à jour filterMo / rembMo / cashMo selon curView
    → fonctionne sur : dashboard, list, remb, cash

[x] Retour haptique — supprimé
    ✕ navigator.vibrate() non supporté sur iOS Safari ni sur Windows
      → feature inutile sur les deux seules plateformes utilisées

[x] Badge compteur sur l'onglet nav Todo : point rouge ou chiffre si tâches
    en retard ou haute priorité non cochées
    → ajouter un <span> dans le nav-btn Todo (index.html), mettre à jour
       à la fin de chaque renderTodo() et toggleTodo()

[x] Widget "tâches urgentes" sur le dashboard : 2–3 items max (retard + haute
    priorité), lien direct vers la vue Todo
    → _urgentTodos filtre todoItems (!done + overdue ou priority=high),
       affiché sous le compte à rebours dans renderDash()

[x] Compte à rebours fin de stage affiché sur le dashboard
    (ex. "il reste 47 jours — fin le 31 août")
    → new Date('2026-08-31') - new Date(), déjà codé endStr = '2026-08-31'


── DONNÉES & CALCULS ──────────────────────────────────────────────────────────

[x] Taux de change TND éditable depuis les réglages
    → fetchRate() + saveRate() déjà implémentés, bouton "↻ Actuel" appelle
       open.er-api.com — complètement fait

[x] Taux USD hardcodé (0.92 dans helpers.js toEur())
    → ajouter settings.rateUSD, lire data.rates.USD dans fetchRate() qui
       utilise déjà open.er-api.com/v6/latest/EUR (même requête que TND)

[x] OPCO par mois : bug de plafonnement
    → monthlyOpco recalculé avec quotas{} initialisés à getPlafond() par catégorie
    → chaque mois consomme son quota puis met à jour quotas[c.id] = restant
    → les mois suivants héritent du quota restant (quotas épuisés → 0€ OPCO)
    → totalRembCapped (vue "all") était déjà correct (somme globale → ok)


── FONCTIONNALITÉS ────────────────────────────────────────────────────────────

[x] Recherche dans la liste des dépenses
    → barre de recherche DÉJÀ présente (index.html l.85–88)
    → fix : étendre filteredExpenses() pour chercher aussi dans enseigne
       et par montant (actuellement : desc + catégorie uniquement)

[x] Comparaison mois : sur le dashboard, afficher mois précédent vs mois actuel
    (delta dépenses, delta remboursable)
    → carte "vs Juin/Juil/..." insérée entre stat-group et barre budget
    → _prevMoIdx = MONTHS.indexOf(filterMo) - 1, calcul _deltaDep / _deltaRemb
    → visible uniquement pour juin / juil / août (pas all, pas mai)

[x] Statistiques dépenses : jour de la semaine le plus dépensier, catégorie
    la plus fréquente — carte collapsible sur le dashboard
    → dashStatsOpen (state.js), toggle bouton ▼/▲ dans renderDash()
    → mini graphe barres par jour (Lun→Dim), barre accent = jour max
    → tuile catégorie la plus fréquente + tuile dépense la plus élevée

[x] Champ notes sur une tâche Todo (contexte court sous le titre, optionnel)
    → textarea "NOTES" dans le formulaire d'édition (renderTodoItem edit mode)
    → saveTodoEdit() lit tp-notes-{id}, stocke t.notes (null si vide)
    → affiché en gris sous le titre en vue normale (masqué si tâche cochée)

[x] Lier une tâche Todo à une dépense existante
    (ex. "vérifier ce reçu" → pointe vers la dépense)
    → section "DÉPENSE LIÉE" dans le formulaire d'édition : champ caché tp-exp-{id},
       picker recherche (filterTodoExpPicker) + selectTodoExp + clearTodoExp sans re-render
    → affichage en vue normale : chip accent cliquable → openDetail(), stopPropagation
    → saveTodoEdit lit tp-exp-{id}, stocke t.expenseId (null si vide)

[x] Alertes budget : toast + indicateur visuel quand on dépasse 80% du budget
    d'une catégorie
    → toast décalé 400ms après "Dépense ajoutée !" si franchissement 80% ou 100%
       (détecte le crossing avant/après, uniquement sur nouvelles dépenses)
    → icône ⚠️ / 🚨 sur le montant de chaque barre de catégorie dans le dashboard

[x] Récurrence : rappel Todo lié à un modèle (pas de création automatique)
    → recurringDay (1–28) sur les templates, éditable via icône ↻ sur chaque chip
    → checkRecurringReminders() dans init() : crée un todo priority=medium, dueDate=today
       si recurringDay === today.getDate() et aucun rappel déjà créé aujourd'hui (reminderId)
    → bouton "Appliquer le modèle" sur les todos reminderId → ouvre modal + applique template + coche todo

[x] Export PDF dossier OPCO (justificatifs + tableau récap)
    → exportRembPDF() dans render-remb.js : HTML → Blob → window.open()
    → page 1 : tableau récap (date/cat/desc/montants/reçu/thumb) + total + plafond OPCO
    → pages suivantes : une page par dépense avec justificatif en grand (page-break-before)
    → bouton PDF dans index.html (icône document) à côté de Excel et ZIP
    → fallback si popup bloquée : téléchargement .html + toast explicatif

[x] Graphique évolution solde net (dépenses − gains) dans le temps
    → gainPoints calculés dans buildProgressionData() : filtre gains, accumulation cumGain par bucket
    → maxV inclut gainPoints si non vides ; gainPath tracé en vert pointillé (stroke-dasharray="6,3")
    → gainAreaPath : remplissage vert transparent sous la courbe gains (linearGradient gain-grad)
    → dots verts r=2.5 sur les points visibles (même step que les dépenses)
    → légende dynamique : Dép. (accent) + Budget (gris tireté) + Gains (vert, affiché seulement si hasGains)


── TECHNIQUE ──────────────────────────────────────────────────────────────────

[x] Sync Gist : inclure les photos dans la sauvegarde cloud
    → second fichier "mi-photos.json" dans le même Gist via PATCH séparé
    → syncPhotosToGist() : lit IDB, filtre IDs valides, limite 9 Mo → toast si dépassé
    → loadPhotosFromGist() : lit mi-photos.json, savePhotoIDB() pour chaque entrée, attache en mémoire
    → settings.syncPhotos (bool, default false) — toggle dans Réglages → Sauvegarde Gist
    → auto-déclenché après syncToGist() et loadFromGist() si settings.syncPhotos=true
    → boutons manuels "Sauvegarder photos" / "Restaurer photos" dans le panneau Gist

[x] Gestion de conflit Gist : si modif sur deux appareils simultanément
    → settings.lastGistSync (ISO) — mis à jour après chaque syncToGist() réussi et chaque loadFromGist() appliqué
    → conflit détecté si : gistSaved > lastGistSync ET localNb > 0 ET gistNb > 0
    → showConflictDialog() : overlay fullscreen — deux colonnes (Cet appareil / Gist) avec date + nb dépenses
    → "Garder local" → maj lastGistSync pour ne plus re-détecter ; "Charger Gist" → _applyGistData()
    → loadFromGist(autoLoad=true) : bypasse le dialog de conflit (savePAT + saveGistId) ; false = bouton manuel

[x] Cash : suivi total déjà en place
    → render-cash.js affiche Retiré / Dépensé / Restant en EUR et TND

[x] index.html découpage en modules JS
    → déjà fait : tout le JS est dans js/ (16 fichiers)
    → les ~3600 lignes restantes dans index.html sont du HTML/CSS, pas de logique


── PRIORITÉ ───────────────────────────────────────────────────────────────────

[x] Calcul budget par mois dans "Budget total stage" : se met à jour en live
    quand on modifie les budgets (updateBudgetLive)
